云存储服务器设计文档

存储空间提供者定时定价，让使用者付费租用空间。

存储提供者使用leveldb保存用户文件

多个磁盘空间，采用一个数据库集合来管理，当一个空间使用完后，保存到下一个空间。

用户上传文件时，先在本地完成文件加密；再上传给空间提供者，提供者将文件切片后保存到数据库。

每个文件由一个文件索引和多个数据分片组成，文件索引中保存了每个数据分片的ID。

每个用户的个人信息和文件索引使用不同的LEVELDB_KEY隔开，方便查询、以及过期删除。

用户文件层级关系通过文件索引包含另一个文件索引的方式来体现。